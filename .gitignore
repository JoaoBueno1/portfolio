# dependencias
/node_modules
/.pnp
.pnp.*

# build
/.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# ambiente — nunca commitar valor real
.env
.env.local
.env*.local

# deploy
.vercel

# sistema
.DS_Store
*.pem

# scratch (gaveta 3 — sondagem descartavel, nunca commitada)
/scratch
_*.ts
debug-*.ts
check-*.ts

# dado de cliente NUNCA entra no repo, nem "so para depurar"
*.csv
*.tsv
*.xlsx
